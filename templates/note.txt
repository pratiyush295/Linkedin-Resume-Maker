<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ contact_info.name }}'s Resume</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            line-height: 1.6;
        }
        h1 {
            text-align: center;
        }
        .section {
            margin-bottom: 20px;
        }
        .section h2 {
            border-bottom: 2px solid #333;
            padding-bottom: 5px;
        }
        .contact-info, .education, .experience, .skills, .projects, .additional-info {
            margin-bottom: 20px;
        }
        .contact-info p, .education ul, .experience ul, .skills ul, .projects ul, .additional-info ul {
            margin: 0;
            padding: 0;
        }
        .education li, .experience li, .projects li {
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <h1>{{ contact_info.name }}</h1>
    <div class="contact-info section">
        <p><strong>Location:</strong> {{ contact_info.location }}</p>
        <p><strong>Phone:</strong> {{ contact_info.phone }}</p>
        <p><strong>Email:</strong> {{ contact_info.email }}</p>
        <p><strong>LinkedIn:</strong> <a href="https://{{ contact_info.linkedin }}">Profile</a></p>
    </div>

    <div class="education section">
        <h2>Education</h2>
        <ul>
            {% for edu in education %}
                <li><strong>{{ edu.degree }}</strong> from {{ edu.institution }} ({{ edu.graduation_date }})</li>
            {% endfor %}
        </ul>
    </div>

    <div class="experience section">
        <h2>Professional Experience</h2>
        <ul>
            {% for exp in professional_experience %}
                <li>
                    <strong>{{ exp.position }}</strong> at {{ exp.company }} ({{ exp.start_date }} - {{ exp.end_date }})
                    <ul>
                        {% for desc in exp.description %}
                            <li>{{ desc }}</li>
                        {% endfor %}
                    </ul>
                </li>
            {% endfor %}
        </ul>
    </div>

    <div class="skills section">
        <h2>Skills</h2>
        <ul>
            <li><strong>Programming Languages:</strong> {{ skills.programming_languages | join(', ') }}</li>
            <li><strong>Technologies:</strong> {{ skills.technologies | join(', ') }}</li>
        </ul>
    </div>

    <div class="projects section">
        <h2>Projects</h2>
        <ul>
            {% for proj in projects %}
                <li>
                    <strong>{{ proj.name }}</strong>
                    <ul>
                        <li><strong>Description:</strong> {{ proj.description | join(', ') }}</li>
                        <li><strong>Tech Stack:</strong> {{ proj.tech_stack | join(', ') }}</li>
                    </ul>
                </li>
            {% endfor %}
        </ul>
    </div>

    <div class="additional-info section">
        <h2>Additional Information</h2>
        <ul>
            {% if additional_info.awards %}
                <li><strong>Awards:</strong> {{ additional_info.awards | join(', ') }}</li>
            {% endif %}
            {% if additional_info.certifications %}
                <li><strong>Certifications:</strong> {{ additional_info.certifications | join(', ') }}</li>
            {% endif %}
            {% if additional_info.extracurricular %}
                <li><strong>Extracurricular:</strong> {{ additional_info.extracurricular | join(', ') }}</li>
            {% endif %}
            {% if additional_info.languages %}
                <li><strong>Languages:</strong> {{ additional_info.languages | join(', ') }}</li>
            {% endif %}
        </ul>
    </div>
</body>
</html>
